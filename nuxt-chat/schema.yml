# ?はnullでもいいことを表している。

users:
  documentID: auto
    name: String
    email: String
    avatorURL: String?
    groupId: map

groups:
  documentID: auto
    name: String
    description: String
    userIds: map
      messages: subCollection
        documentID: auto
          message: String
          imgae: String?
          userId: refs
